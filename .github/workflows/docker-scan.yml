name: Docker Image Security Scan

on:
  push:
    branches: [ master ]

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: List files in repo root
        run: ls -l

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker image
        run: docker build -t cysec-ci-cd-app .

      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin

      - name: Run Trivy scan (fail on high/critical)
        run: trivy image --exit-code 1 --no-progress cysec-ci-cd-app

      - name: Install Grype
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin

      - name: Run Grype scan (fail on high/critical)
        run: grype --exit-code 1 cysec-ci-cd-app

      - name: Send Discord Alert on Failure
        if: failure()
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d '{
            "username": "CI-CD Security Bot",
            "content": "**🔴 Security Alert:**\nTool: Trivy/Grype\nStatus: ❌ FAILED\nSeverity: HIGH/CRITICAL\nRepo: '${{ github.repository }}'\nCommit: '${{ github.sha }}'"
          }' \
          ${{ secrets.DISCORD_WEBHOOK }}
